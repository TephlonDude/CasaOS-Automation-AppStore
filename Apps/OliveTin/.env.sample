# Copy to .env and set values for your deployment
# Set a pinned image tag or digest for OliveTin to avoid unexpected upgrades
OLIVETIN_IMAGE=jamesread/olivetin:REPLACE_WITH_TAG_OR_DIGEST

# Example:
# OLIVETIN_IMAGE=jamesread/olivetin:v1.2.3

# If OliveTin requires any runtime secrets, place them here.
# e.g.
# OLIVETIN_SOME_SECRET=changeme
